import {
  ResultStepPlaceholder,
  type ResultStepProps,
} from "./ResultStepPlaceholder";

export function ReturningHomeStep(props: ResultStepProps) {
  return <ResultStepPlaceholder step="returning-home" {...props} />;
}
