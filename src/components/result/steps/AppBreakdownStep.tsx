import {
  ResultStepPlaceholder,
  type ResultStepProps,
} from "./ResultStepPlaceholder";

export function AppBreakdownStep(props: ResultStepProps) {
  return <ResultStepPlaceholder step="app-breakdown" {...props} />;
}
