import {
  ResultStepPlaceholder,
  type ResultStepProps,
} from "./ResultStepPlaceholder";

export function HouseEquivalentStep(props: ResultStepProps) {
  return <ResultStepPlaceholder step="house-equivalent" {...props} />;
}
