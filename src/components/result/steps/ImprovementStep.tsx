import {
  ResultStepPlaceholder,
  type ResultStepProps,
} from "./ResultStepPlaceholder";

export function ImprovementStep(props: ResultStepProps) {
  return <ResultStepPlaceholder step="improvement" {...props} />;
}
