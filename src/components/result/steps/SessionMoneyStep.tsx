import {
  ResultStepPlaceholder,
  type ResultStepProps,
} from "./ResultStepPlaceholder";

export function SessionMoneyStep(props: ResultStepProps) {
  return <ResultStepPlaceholder step="session-money" {...props} />;
}
