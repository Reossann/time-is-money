import {
  ResultStepPlaceholder,
  type ResultStepProps,
} from "./ResultStepPlaceholder";

export function LifetimeMoneyStep(props: ResultStepProps) {
  return <ResultStepPlaceholder step="lifetime-money" {...props} />;
}
